<Window x:Class="Layout.Views.GridToolView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Chuyển đổi Grid 2D/3D" Height="150" Width="300"
        WindowStartupLocation="CenterScreen"
        Topmost="True">
    
    <Grid Margin="20">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <TextBlock Text="2D" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16" FontWeight="Bold"/>

            <ToggleButton IsChecked="{Binding Is3DSelected, Mode=TwoWay}" 
                          Command="{Binding ApplyGridStateCommand}"
                          Width="60" Height="30">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Content" Value="←"/>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content" Value="→"/>
                                <Setter Property="Background" Value="LightBlue"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>

            <TextBlock Text="3D" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="16" FontWeight="Bold"/>
            
        </StackPanel>
    </Grid>
</Window>